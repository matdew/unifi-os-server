apiVersion: v1
kind: Service
metadata:
  name: mongodb-svc
  namespace: unifi
  labels:
    app: mongodb
spec:
  selector:
    app: mongodb
  ports:
  - name: mongo
    port: 27017
    targetPort: 27017
    protocol: TCP
  type: ClusterIP
